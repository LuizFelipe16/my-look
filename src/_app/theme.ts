import { styledPresets, border, padding, font, colors, size, presets } from '../_lib/web';

export const theme = {
  styled: styledPresets,
  presets,
  colors,
  font,
  border,
  padding,
  size: size,
  spacing: {
    size: size,
  },
};
